name: quick_alarm
description: App to setup quick alarms
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  android_alarm_manager: ^0.4.5+11
  flutter_ringtone_player: ^2.0.0
  shared_preferences: ^0.5.7+3
  rxdart: ^0.24.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
